server:
  port: 8020

spring:
  profiles:
    active: dev
  application:
    name: spring-cloud-zipkin

  cloud:
    inetutils:
      ignoredInterfaces:
        - docker0
        - veth.*
      preferredNetworks:
        - 192.168